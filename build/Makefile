all:
	(cd new_test;     make -f Makefile)

clean:
	(cd new_test;     make -f Makefile clean)

allclean:
	(cd new_test;     make -f Makefile allclean)
	rm -f Makefile

distclean:
	(cd new_test;     make -f Makefile distclean)
	rm -f Makefile
